[package]
name = "dsfr"
version = "0.1.0"
edition = "2024"

[dependencies]
leptos.workspace = true
web-sys = { workspace = true, features = ["Window", "Storage", "DomTokenList", "MediaQueryList", "MediaQueryListEvent"] }

[features]
default = []
hydrate = ["leptos/hydrate"]
ssr = ["leptos/ssr"]
